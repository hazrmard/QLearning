"""
This module defines the System class which contains a model to be simulated,
defines variations in the model for different states, encodes results into
states, and generates reward and transition matrices.
"""
import os
os.environ['LANG'] = 'en_US.UTF-8'
import ahkab


class System:

    def __init__(self):
        pass


    def read_netlist(self, f):
        pass


    def encode(self):
        pass
